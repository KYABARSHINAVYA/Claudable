export async function createContainer(projectId: string) {
  console.log(`Creating container for ${projectId}`);
}

export async function destroyContainer(projectId: string) {
  console.log(`Destroying container for ${projectId}`);
}